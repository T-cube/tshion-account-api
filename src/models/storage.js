import _ from 'underscore';

import db from 'lib/database';

export default class Storage {

  constructor(config) {
    this.config = config;
  }

  check(company, fileSize) {
    
  }

  update(company) {

  }

}
